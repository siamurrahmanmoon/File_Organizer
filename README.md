
---

### 🔄 `README.md` এ যে অংশটি যোগ করবেন

আপনার আগের `README.md` ফাইলের **Installation** সেকশনের পরে এই নতুন সেকশনটি যোগ করে দিন:

```markdown
## ️ Building the Windows App (.exe)

If you want to compile this project into a standalone Windows executable (`.exe`) so it can run without Python installed:

1. **Install PyInstaller**:
   ```bash
   pip install pyinstaller